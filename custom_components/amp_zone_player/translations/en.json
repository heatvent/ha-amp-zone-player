{
  "config": {
    "step": {
      "user": {
        "title": "Matrix Amplifier Zone Player",
        "description": "Choose the decoder (real streamer) and the amp zone media players to expose in Music Assistant.",
        "data": {
          "decoder": "Decoder (real streamer)",
          "zones": "Amp zone media players",
          "name_prefix": "Name prefix for facade players (optional — leave blank)",
          "name": "Hub name (optional — blank becomes Amp zones; does not prefix players)"
        },
        "data_description": {
          "name": "Shown only as the integration entry / device label. Player names stay short (e.g. Bar Speakers)."
        }
      },
      "source": {
        "title": "Amp input when turning on",
        "description": "{hint}\n\nThe list is built from each zone’s **source_list** (the same names you see in the zone more-info Source dropdown). Pick one, leave as None, or type the exact plain-text label if it is missing.",
        "data": {
          "source": "Amp input / source name"
        },
        "data_description": {
          "source": "Plain text matching the zone source list (e.g. WiiM Pro). Not a media_player entity ID."
        }
      }
    },
    "error": {
      "entity_not_found": "One or more entities were not found",
      "no_zones": "Select at least one zone",
      "decoder_in_zones": "Decoder cannot also be listed as a zone"
    },
    "abort": {
      "already_configured": "This Matrix Amplifier Zone Player entry is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Matrix Amplifier Zone Player options",
        "description": "Update the decoder and amp zones. Next you will pick the amp input source name.",
        "data": {
          "decoder": "Decoder (real streamer)",
          "zones": "Amp zone media players",
          "name_prefix": "Name prefix for facade players (optional)"
        }
      },
      "source": {
        "title": "Amp input when turning on",
        "description": "{hint}\n\nThe list is built from each zone’s **source_list**. Pick one, leave as None, or type the exact plain-text label.",
        "data": {
          "source": "Amp input / source name"
        },
        "data_description": {
          "source": "Plain text matching the zone source list (e.g. WiiM Pro). Not a media_player entity ID."
        }
      }
    },
    "error": {
      "entity_not_found": "One or more entities were not found",
      "no_zones": "Select at least one zone",
      "decoder_in_zones": "Decoder cannot also be listed as a zone"
    }
  }
}
